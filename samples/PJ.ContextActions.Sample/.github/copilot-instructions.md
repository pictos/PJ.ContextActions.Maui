# Copilot Instructions

## Project Guidelines
- When working with platform-specific code in .NET MAUI, especially on Windows (WinUI), it's crucial to use the platform's native data binding mechanism (e.g., Microsoft.UI.Xaml.Data.Binding) for UI controls, not the MAUI Binding class.